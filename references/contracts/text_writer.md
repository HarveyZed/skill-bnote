# 派单：信息流整理稿（bnote 契约 text v1）

你在把**一集信息流视频**（口播 / 播客 / 访谈：画面是动画、配图、出镜等无关内容，**没有幻灯片**）的字幕
整理成可读的 Markdown 整理稿。产出目录：{{OUT_DIR}} ｜ 视频：{{TITLE}}（{{DURATION}}）｜
共 {{CHUNK_COUNT}} 块 / {{PARA_COUNT}} 段。

## 材料怎么读（渐进式披露，别一次吞完）

1. 素材被切成 {{CHUNK_COUNT}} 块，每块一个文件（见下表）。**一块一块来**：读第 N 块素材 → 写第 N 块产出 → 再读下一块；
2. 不要跳块、也不要把所有块一次读完（长视频会撑爆上下文，而且写得越靠后越糊）；
3. 每块素材里已经给出：本块覆盖的段落、每段的**工具打的锚点**、以及该段的字幕原文。

| 块 | 时间 | 段落 | 字数 | 素材 | 产出 |
|---|---|---|---|---|---|
{{CHUNK_TABLE}}

## 视频背景（视频页元信息：简介 / 标签 / 分区）

{{VIDEO_META}}

## 你产出什么

`{{OUT_DIR}}/text/NN.md` —— 每块一个文件，**纯正文 Markdown**：

- 段落标题写成 `## [00:12:30] 自己拟的小标题`：锚点**必须**用素材里给的那个（工具打的）；
  可以合并相邻段落（同一话题连贯时），**合并后用被合并段落里【最早】那一段的锚点**（用中间或靠后的锚点会把内容标到更晚的时间上，
  校验会以「首句来自更早的段落」给出警告）；
- 正文把该段字幕整理成通顺的中文：补标点、断句、必要时调整语序；
- **逐段覆盖、不摘要、不压缩**：口头语可以轻量收敛（重复的“那个那个”可删），但**不许**把几段并成一句结论、
  不许省略例子与细节、不许跳过听不清的地方（听不清就照实标出来）。

## 硬性约束

- **事实来源只有字幕与人写的元信息**：讲师没说的不写；你自己的推断必须标「（推断）」；
- **字幕不是准绳**：平台 AI 轨与 UP 上传的 CC 都是语音识别、都可能没校对。判断用字时优先看**人写的元信息**（标题/简介/标签）；
  冲突或拿不准就就地括注存疑（例如音近疑为某词），**不要**因为字幕里出现某写法就认定它对，也不要静默统一；
- **不编造**：人名、机构、产品名、数字一律不许补（数字拿不准就保留原样并标注）；
- 术语与写法：

{{TERM_POLICY}}

- 排版：

{{FORMAT_RULES}}

- 不要写元信息：没有前言、没有目录、没有“本次整理…”之类的话——拼装与导航由工具负责。

## 写完自检

```bash
{{CHECK_CMD}}
```

校验会拦：自编时间锚点（不在工具给的段落起点里）、锚点不单调、整理稿字数少于字幕正文的约定比例（视为摘要压缩）。

## 回报格式

- 写了几块、每块字数；
- 合并了哪些段落（合并处一句理由）；
- 拿不准的地方（时间点 + 疑问）；
- 字幕里明显的 ASR 错字（原文 → 你写成什么 → 依据）。
